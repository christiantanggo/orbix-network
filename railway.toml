[build]
builder = "NIXPACKS"
buildCommand = "cd apps/worker && pip install -r requirements.txt"

[deploy]
startCommand = "cd apps/worker && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

